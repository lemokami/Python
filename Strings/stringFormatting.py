#this program uses fstrings to format the strings
name = input("Enter your name:")

age = int(input("Enter your age:"))

print(f"Hello,{name} you are {(age+1)} next year") #printing the strings using formatted string literals
